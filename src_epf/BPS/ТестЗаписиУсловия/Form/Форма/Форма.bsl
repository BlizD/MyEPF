
&НаКлиенте
Процедура Команда1(Команда)
	СохранитьНаСервере();
КонецПроцедуры

&НаСервере
Процедура СохранитьНаСервере() Экспорт
	СпрОбъект = Объект.СсылкаНаОбъект.ПолучитьОбъект();
	СпрОбъект.Записать();
КонецПроцедуры //СохранитьНаСервере()
